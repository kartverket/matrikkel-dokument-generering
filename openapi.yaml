# Denne filen er automatisk generert av `bun run generate:openapi`.
openapi: 3.0.3
info:
  title: Matrikkel Dokument Generering API
  description: |-
    API for å generere PDF-dokumenter fra matrikkelrapporter. Tjenesten tar imot
    en strukturert JSON, renderer den til HTML og konverterer resultatet til PDF via
    [Gotenberg](https://github.com/kartverket/pdf-generator).

    Følgende rapporter støttes:
    - BYG0011 – Byggrapport
  version: 1.0.0
  license:
    name: Se LICENCE.md
    url: https://github.com/kartverket/matrikkel-dokument-generering/blob/main/LICENCE.md
servers:
  - url: http://localhost:3000
    description: Lokal utvikling
  - url: http://localhost:8087
    description: Container
security: []
tags:
  - name: Dokument
    description: Generering av PDF-dokumenter
  - name: Internal
    description: Helsesjekk-endepunkter
components:
  schemas:
    ValidationErrorResponse:
      type: object
      properties:
        errors:
          type: object
          properties:
            valid:
              type: boolean
              enum:
                - false
            errors:
              type: object
              additionalProperties:
                type: array
                items:
                  type: string
          required:
            - valid
            - errors
      required:
        - errors
      description: Returneres når byggrapporten ikke passerer validering. Feilene er gruppert etter feltnavn.
    PdfErrorResponse:
      type: object
      properties:
        error:
          type: string
          example: PDF-generering feilet
        details:
          type: string
          example: "Gotenberg svarte med 500: ..."
      required:
        - error
        - details
      description: Returneres når konvertering til PDF feiler.
    ByggRapport:
      allOf:
        - $ref: "#/components/schemas/Rapport"
        - type: object
          properties:
            rapportType:
              type: string
              enum:
                - BYG0011
              description: Rapporttype. Må være `BYG0011`.
            utvalgskriterier:
              $ref: "#/components/schemas/Utvalgskriterier"
            bygninger:
              type: array
              items:
                $ref: "#/components/schemas/Bygning"
          required:
            - utvalgskriterier
            - bygninger
      description: Byggrapport som skal genereres til PDF.
    Kommune:
      type: object
      properties:
        nr:
          type: string
          minLength: 1
          example: "0301"
        navn:
          type: string
          minLength: 1
          example: Oslo
      required:
        - nr
        - navn
    Locale:
      type: string
      enum:
        - nb
        - nn
      description: Målform for dokumentet – bokmål (`nb`) eller nynorsk (`nn`).
    Utvalgskriterier:
      type: object
      properties:
        bestaaendeBygg:
          type: boolean
        utgaatteBygg:
          type: boolean
        bygninger:
          type: boolean
        bygningsendringer:
          type: boolean
        frededeBygninger:
          type: string
          minLength: 1
        matrikkelenhet:
          $ref: "#/components/schemas/MatrikkelenhetGnrBnr"
        subrapporter:
          type: object
          properties:
            etasjer:
              type: boolean
            bruksenheter:
              type: boolean
            tiltakshavere:
              type: boolean
            kontaktpersoner:
              type: boolean
            hjemmelshavere:
              type: boolean
            kulturminner:
              type: boolean
          required:
            - etasjer
            - bruksenheter
            - tiltakshavere
            - kontaktpersoner
            - hjemmelshavere
            - kulturminner
      required:
        - bestaaendeBygg
        - utgaatteBygg
        - bygninger
        - bygningsendringer
        - frededeBygninger
        - matrikkelenhet
        - subrapporter
    MatrikkelenhetGnrBnr:
      type: object
      properties:
        gnr:
          type: number
        bnr:
          type: number
      required:
        - gnr
        - bnr
    Bygning:
      type: object
      properties:
        id:
          type: number
        bygningsnr:
          type: string
          minLength: 1
        bygningstype:
          $ref: "#/components/schemas/Bygningstype"
        adresseverdig:
          type: boolean
        naeringsgruppe:
          type: string
          minLength: 1
        matrikkelenhet:
          type: string
          minLength: 1
        endringer:
          type: array
          items:
            $ref: "#/components/schemas/Bygningsendring"
      required:
        - id
        - bygningsnr
        - bygningstype
        - adresseverdig
        - naeringsgruppe
        - matrikkelenhet
        - endringer
    Bygningstype:
      type: object
      properties:
        kode:
          type: number
        navn:
          type: string
          minLength: 1
      required:
        - kode
        - navn
    Bygningsendring:
      type: object
      properties:
        id:
          type: number
        bygningId:
          type: number
        lopenr:
          type: number
        endringskode:
          type: string
          nullable: true
        bygningsstatus:
          $ref: "#/components/schemas/Bygningsstatus"
        antallBoenheter:
          type: number
        bruksareal:
          $ref: "#/components/schemas/ArealFordeling"
        bruttoareal:
          $ref: "#/components/schemas/ArealFordeling"
        bebygdAreal:
          type: number
        koordinat:
          $ref: "#/components/schemas/Koordinat"
        datoer:
          $ref: "#/components/schemas/Bygningsdatoer"
        etasjeplan:
          type: array
          items:
            $ref: "#/components/schemas/Bygningsetasje"
        bruksenheter:
          type: array
          items:
            $ref: "#/components/schemas/Bruksenhet"
        tiltakshavere:
          type: array
          items:
            $ref: "#/components/schemas/Tiltakshaver"
          default: []
        kontaktpersoner:
          type: array
          items:
            $ref: "#/components/schemas/Kontaktperson"
          default: []
        hjemmelshavere:
          type: array
          items:
            $ref: "#/components/schemas/Hjemmelshaver"
        kulturminner:
          type: array
          items:
            $ref: "#/components/schemas/Kulturminne"
          default: []
      required:
        - id
        - bygningId
        - lopenr
        - endringskode
        - bygningsstatus
        - antallBoenheter
        - bruksareal
        - bruttoareal
        - bebygdAreal
        - koordinat
        - datoer
        - etasjeplan
        - bruksenheter
        - hjemmelshavere
    Bygningsstatus:
      type: object
      properties:
        kode:
          type: number
        kortkode:
          type: string
          minLength: 1
        navn:
          type: string
          minLength: 1
        bestaaende:
          type: boolean
      required:
        - kode
        - kortkode
        - navn
        - bestaaende
    ArealFordeling:
      type: object
      properties:
        bolig:
          type: number
        annet:
          type: number
        totalt:
          type: number
      required:
        - bolig
        - annet
        - totalt
    Koordinat:
      type: object
      properties:
        nord:
          type: number
        ost:
          type: number
      required:
        - nord
        - ost
    Bygningsdatoer:
      type: object
      properties:
        rammetillatelse:
          type: string
          nullable: true
        igangsettingstillatelse:
          type: string
          nullable: true
        midlertidigBrukstillatelse:
          type: string
          nullable: true
        ferdigattest:
          type: string
          nullable: true
        tattIBruk:
          type: string
          nullable: true
        utgaattRevet:
          type: string
          nullable: true
      required:
        - rammetillatelse
        - igangsettingstillatelse
        - midlertidigBrukstillatelse
        - ferdigattest
        - tattIBruk
        - utgaattRevet
    Bygningsetasje:
      type: object
      properties:
        etasjeplan:
          type: string
          minLength: 1
        etasje:
          type: number
        antallBoenheter:
          type: number
        bruksareal:
          $ref: "#/components/schemas/ArealFordeling"
        bruttoareal:
          $ref: "#/components/schemas/ArealFordeling"
      required:
        - etasjeplan
        - etasje
        - antallBoenheter
        - bruksareal
        - bruttoareal
    Bruksenhet:
      type: object
      properties:
        bruksenhetsnr:
          type: string
          nullable: true
        type:
          type: string
          minLength: 1
        matrikkelenhet:
          $ref: "#/components/schemas/MatrikkelenhetGnrBnr"
        bruksareal:
          type: number
        antallRom:
          type: number
        kjokkentilgang:
          type: boolean
          nullable: true
        antallBad:
          type: number
        antallWc:
          type: number
        adresse:
          type: string
          nullable: true
        egenregistrert:
          $ref: "#/components/schemas/EgenregistrerteFelter"
      required:
        - bruksenhetsnr
        - type
        - matrikkelenhet
        - bruksareal
        - antallRom
        - kjokkentilgang
        - antallBad
        - antallWc
        - adresse
        - egenregistrert
    EgenregistrerteFelter:
      type: object
      nullable: true
      properties:
        byggeaar:
          type: number
          nullable: true
        energikilder:
          type: array
          items:
            $ref: "#/components/schemas/Egenregistrert"
        oppvarming:
          type: array
          items:
            $ref: "#/components/schemas/Egenregistrert"
        vannforsyning:
          allOf:
            - $ref: "#/components/schemas/Egenregistrert"
            - nullable: true
        avlop:
          allOf:
            - $ref: "#/components/schemas/Egenregistrert"
            - nullable: true
      required:
        - byggeaar
        - energikilder
        - oppvarming
        - vannforsyning
        - avlop
    Egenregistrert:
      type: object
      properties:
        data:
          type: string
          minLength: 1
        erGjeldende:
          type: boolean
        gyldighetsperiode:
          $ref: "#/components/schemas/Gyldighetsperiode"
      required:
        - data
        - erGjeldende
        - gyldighetsperiode
    Gyldighetsperiode:
      type: object
      properties:
        gyldigFra:
          type: number
          nullable: true
        gyldigTil:
          type: number
          nullable: true
      required:
        - gyldigFra
        - gyldigTil
    Tiltakshaver:
      type: object
      properties:
        rolle:
          type: string
          minLength: 1
        eierIdent:
          type: string
          minLength: 1
        navn:
          type: string
          minLength: 1
        adresselinje1:
          type: string
          nullable: true
        adresselinje2:
          type: string
          nullable: true
        adresselinje3:
          type: string
          nullable: true
        postnummeromradenr:
          type: string
          nullable: true
        postnummeromradenavn:
          type: string
          nullable: true
        land:
          type: string
          nullable: true
        bruksenhetsnr:
          type: string
          nullable: true
        datofra:
          type: string
          nullable: true
        datofraSOSI:
          type: string
          nullable: true
        harDatofra:
          type: boolean
          default: false
        kategorikode:
          type: string
          nullable: true
        kontaktpersonKode:
          type: string
          nullable: true
        statuskode:
          type: string
          nullable: true
        eierErUtgatt:
          type: boolean
          default: false
      required:
        - rolle
        - eierIdent
        - navn
        - adresselinje1
        - adresselinje2
        - adresselinje3
        - postnummeromradenr
        - postnummeromradenavn
        - land
        - bruksenhetsnr
        - datofra
        - datofraSOSI
        - kategorikode
        - kontaktpersonKode
        - statuskode
    Kontaktperson:
      type: object
      properties:
        rolle:
          type: string
          minLength: 1
        eierIdent:
          type: string
          minLength: 1
        navn:
          type: string
          minLength: 1
        adresselinje1:
          type: string
          nullable: true
        adresselinje2:
          type: string
          nullable: true
        adresselinje3:
          type: string
          nullable: true
        postnummeromradenr:
          type: string
          nullable: true
        postnummeromradenavn:
          type: string
          nullable: true
        land:
          type: string
          nullable: true
        bruksenhetsnr:
          type: string
          nullable: true
        datofra:
          type: string
          nullable: true
        datofraSOSI:
          type: string
          nullable: true
        harDatofra:
          type: boolean
          default: false
        kategorikode:
          type: string
          nullable: true
        kontaktpersonKode:
          type: string
          nullable: true
        statuskode:
          type: string
          nullable: true
        eierErUtgatt:
          type: boolean
          default: false
      required:
        - rolle
        - eierIdent
        - navn
        - adresselinje1
        - adresselinje2
        - adresselinje3
        - postnummeromradenr
        - postnummeromradenavn
        - land
        - bruksenhetsnr
        - datofra
        - datofraSOSI
        - kategorikode
        - kontaktpersonKode
        - statuskode
    Hjemmelshaver:
      type: object
      properties:
        eierIdent:
          type: string
          minLength: 1
        navn:
          type: string
          minLength: 1
        andelTeller:
          type: number
          nullable: true
        andelNevner:
          type: number
          nullable: true
        eierforhold:
          type: string
          minLength: 1
        adresselinje1:
          type: string
          nullable: true
        adresselinje2:
          type: string
          nullable: true
        adresselinje3:
          type: string
          nullable: true
        postnummer:
          type: string
          nullable: true
        poststed:
          type: string
          nullable: true
        land:
          type: string
          nullable: true
        statuskode:
          type: string
          nullable: true
        eierErUtgatt:
          type: boolean
          default: false
        datofra:
          type: string
          nullable: true
        datotil:
          type: string
          nullable: true
        kategorikode:
          type: string
          nullable: true
        bruksenhetsnr:
          type: string
          nullable: true
        harAndel:
          type: boolean
          nullable: true
        erSelveier:
          type: boolean
          nullable: true
        matrikkelenhet:
          $ref: "#/components/schemas/MatrikkelenhetFull"
      required:
        - eierIdent
        - navn
        - andelTeller
        - andelNevner
        - eierforhold
        - adresselinje1
        - adresselinje2
        - adresselinje3
        - postnummer
        - poststed
        - land
        - statuskode
        - datofra
        - datotil
        - kategorikode
        - bruksenhetsnr
        - harAndel
        - erSelveier
        - matrikkelenhet
    MatrikkelenhetFull:
      type: object
      nullable: true
      properties:
        gnr:
          type: number
        bnr:
          type: number
        fnr:
          type: number
          nullable: true
        snr:
          type: number
          nullable: true
      required:
        - gnr
        - bnr
        - fnr
        - snr
    Kulturminne:
      type: object
      properties:
        id:
          type: string
          minLength: 1
        navn:
          type: string
          minLength: 1
        status:
          type: string
          minLength: 1
        kategori:
          type: string
          minLength: 1
      required:
        - id
        - navn
        - status
        - kategori
    Rapport:
      type: object
      properties:
        rapportType:
          type: string
          minLength: 1
        tittel:
          type: string
          minLength: 1
        kommune:
          $ref: "#/components/schemas/Kommune"
        koordinatsystem:
          type: string
          minLength: 1
          example: EUREF89 UTM sone 32
        locale:
          $ref: "#/components/schemas/Locale"
        generertTidspunkt:
          type: string
          minLength: 1
          example: 2026-07-10T12:00:00Z
      required:
        - rapportType
        - tittel
        - kommune
        - koordinatsystem
        - locale
        - generertTidspunkt
  parameters: {}
paths:
  /internal/isAlive:
    get:
      tags:
        - Internal
      summary: Liveness-probe
      description: Returnerer alltid `OK` når prosessen kjører.
      operationId: isAlive
      responses:
        "200":
          description: Tjenesten svarer.
          content:
            text/plain:
              schema:
                type: string
                enum:
                  - OK
  /internal/isReady:
    get:
      tags:
        - Internal
      summary: Readiness-probe
      description: Returnerer alltid `OK` når prosessen er klar.
      operationId: isReady
      responses:
        "200":
          description: Tjenesten svarer.
          content:
            text/plain:
              schema:
                type: string
                enum:
                  - OK
  /create-document:
    post:
      tags:
        - Dokument
      summary: Generer PDF fra en byggrapport
      description: Validerer innsendt byggrapport mot skjemaet. Ved gyldig input renderes dokumentet til
        HTML og konverteres til PDF. Ved ugyldig input returneres valideringsfeil per felt.
      operationId: createDocument
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ByggRapport"
      responses:
        "200":
          description: PDF-dokumentet ble generert.
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        "400":
          description: Valideringen av byggrapporten feilet.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationErrorResponse"
        "502":
          description: PDF-generering via Gotenberg feilet.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PdfErrorResponse"
